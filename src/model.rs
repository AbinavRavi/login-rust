pub mod model{
    pub struct SignupInfo{
        email: String,
        password: String,
        name: String,
    }

    pub struct LoginInfo{
        email: String,
        password: String
    }
}